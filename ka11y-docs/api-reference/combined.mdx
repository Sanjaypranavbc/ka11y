---
title: "Combined Audit"
description: "POST /api/v1/combined/ · GET /api/v1/combined/{job_id} · GET /api/v1/combined/{job_id}/stream"
---

The combined endpoint is the **primary API**. It runs axe-core (Node) and all Python auditors in parallel, merges findings into a single flat list, and exposes job status via REST polling and Server-Sent Events streaming.

---

## Submit an audit

<ParamField body="url" type="string" required>
  Fully-qualified URL to audit (e.g. `https://example.com`).
</ParamField>

<ParamField body="max_depth" type="integer" default="0">
  How many link-levels deep to crawl. `0` audits only the submitted URL. `1` follows internal links one level deep. Keep low (≤ 2) to avoid long runtimes.
</ParamField>

<ParamField body="wcag_level" type="string" default="AA">
  Conformance target: `"A"` | `"AA"` | `"AAA"`. Controls which axe-core tags are used and filters Python findings by WCAG level.
</ParamField>

<ParamField body="run_ocr" type="boolean" default="true">
  Run EasyOCR on crawled images to detect embedded text before auditing alt attributes.
</ParamField>

<ParamField body="run_image_audit" type="boolean" default="true">
  Enable the WCAG 1.1.1 alt-text auditor.
</ParamField>

<ParamField body="run_form_audit" type="boolean" default="true">
  Enable the WCAG 3.3.1 / 3.3.2 form auditor.
</ParamField>

<ParamField body="run_label_in_name_audit" type="boolean" default="true">
  Enable the WCAG 2.5.3 label-in-name auditor.
</ParamField>

<ParamField body="run_pause_stop_hide_audit" type="boolean" default="true">
  Enable the WCAG 2.2.2 pause/stop/hide auditor.
</ParamField>

<ParamField body="run_target_size_audit" type="boolean" default="true">
  Enable the WCAG 2.5.8 target-size auditor.
</ParamField>

<RequestExample>
```bash cURL
curl -X POST http://localhost:8000/api/v1/combined/ \
  -H "Content-Type: application/json" \
  -d '{
    "url": "https://example.com",
    "wcag_level": "AA",
    "max_depth": 0,
    "run_ocr": true,
    "run_image_audit": true,
    "run_form_audit": true,
    "run_label_in_name_audit": true,
    "run_pause_stop_hide_audit": true,
    "run_target_size_audit": true
  }'
```

```python Python
import httpx

resp = httpx.post("http://localhost:8000/api/v1/combined/", json={
    "url": "https://example.com",
    "wcag_level": "AA",
    "max_depth": 0,
})
job_id = resp.json()["job_id"]
```
</RequestExample>

<ResponseExample>
```json HTTP 202
{
  "job_id": "b5ea3ea7-2808-4f31-b589-f3624faaf6ce",
  "status": "pending",
  "url": "https://example.com",
  "submitted_at": "2026-03-17T17:13:48.409Z",
  "completed_at": null,
  "report_path": null,
  "result": null,
  "error": null,
  "current_stage": null,
  "stages": [],
  "warnings": []
}
```
</ResponseExample>

---

## Poll job status

```
GET /api/v1/combined/{job_id}
```

Returns the full job record. Poll every 3 – 5 s until `status` is `"completed"` or `"failed"`.

<ResponseExample>
```json Completed
{
  "job_id": "b5ea3ea7-...",
  "status": "completed",
  "url": "https://example.com",
  "submitted_at": "2026-03-17T17:13:48Z",
  "completed_at": "2026-03-17T17:18:02Z",
  "report_path": "/app/crawled_images/example_com_0317_1713_combined/combined_report.json",
  "current_stage": null,
  "stages": [
    { "name": "axe_core",        "status": "completed", "findings_count": 42 },
    { "name": "image_audit",     "status": "completed", "findings_count": 8  },
    { "name": "form_audit",      "status": "completed", "findings_count": 3  },
    { "name": "label_in_name",   "status": "completed", "findings_count": 5  },
    { "name": "pause_stop_hide", "status": "completed", "findings_count": 2  },
    { "name": "target_size",     "status": "completed", "findings_count": 12 }
  ],
  "warnings": [],
  "result": {
    "url": "https://example.com",
    "generated_at": "2026-03-17T17:18:02Z",
    "summary": {
      "total_findings": 72,
      "violations": 20,
      "needs_review": 7,
      "passes": 45,
      "by_severity": { "critical": 2, "high": 8, "medium": 7, "low": 3 },
      "by_level": { "A": { "violations": 14 }, "AA": { "violations": 6 } },
      "by_wcag_sc": { "1.1.1": { "violations": 8 }, "4.1.2": { "violations": 5 } },
      "by_source": {
        "axe":    { "violations": 12, "needs_review": 7, "passes": 35 },
        "python": { "violations": 8,  "needs_review": 0, "passes": 10 }
      }
    },
    "violations": [ /* ... finding objects ... */ ],
    "needs_review": [ /* ... */ ],
    "passes": [ /* ... */ ],
    "warnings": []
  }
}
```

```json Running
{
  "job_id": "b5ea3ea7-...",
  "status": "running",
  "current_stage": "image_audit",
  "stages": [
    { "name": "axe_core",    "status": "completed", "findings_count": 42 },
    { "name": "image_audit", "status": "running",   "started_at": "2026-03-17T17:14:00Z" }
  ],
  "warnings": []
}
```

```json Graceful degradation (Node failed, Python succeeded)
{
  "job_id": "b5ea3ea7-...",
  "status": "completed",
  "stages": [
    { "name": "axe_core", "status": "error", "error": "Failed to launch the browser process!" },
    { "name": "image_audit", "status": "completed", "findings_count": 8 }
  ],
  "warnings": [
    "axe_core: Server error '500 Internal Server Error' for url 'http://node:3000/api/v1/analyse-url-flat'"
  ],
  "result": { /* Python-only findings */ }
}
```
</ResponseExample>

---

## Stream job events (SSE)

```
GET /api/v1/combined/{job_id}/stream
```

Returns a `text/event-stream` response. Connect with `EventSource` in the browser or `curl -N` in a terminal.

<Note>
  Connect **immediately after** submitting the job. If you connect after the job has already completed or failed, the stream sends a single terminal event and closes.
</Note>

### Event types

| Event | Payload | When |
|-------|---------|------|
| `stage_start` | `{ stage_name, started_at }` | A stage begins executing |
| `stage_complete` | `{ stage_name, completed_at, findings_count }` | A stage finishes successfully |
| `stage_error` | `{ stage_name, error }` | A stage fails (non-fatal) |
| `job_state` | `{ current_stage, stages[] }` | Sent once on late connect if job is already running |
| `job_complete` | `{ job_id, summary }` | Job finished (poll REST for full `result`) |
| `job_failed` | `{ job_id, error }` | All sources failed |

Heartbeat comment lines (`": keepalive"`) are sent every 25 seconds to prevent proxy timeouts.

<RequestExample>
```bash Terminal
curl -N http://localhost:8000/api/v1/combined/b5ea3ea7-.../stream
```

```javascript Browser
const es = new EventSource(`/api/v1/combined/${jobId}/stream`);

es.addEventListener("stage_start", e => {
  const { stage_name, started_at } = JSON.parse(e.data);
  console.log(`▶ ${stage_name} started`);
});

es.addEventListener("stage_complete", e => {
  const { stage_name, findings_count } = JSON.parse(e.data);
  console.log(`✓ ${stage_name} — ${findings_count} findings`);
});

es.addEventListener("job_complete", async e => {
  es.close();
  const { job_id } = JSON.parse(e.data);
  const res = await fetch(`/api/v1/combined/${job_id}`);
  const full = await res.json();
  console.log(full.result.summary);
});
```
</RequestExample>

<ResponseExample>
```text SSE stream
event: stage_start
data: {"stage_name": "axe_core", "started_at": "2026-03-17T17:13:48Z"}

event: stage_start
data: {"stage_name": "image_audit", "started_at": "2026-03-17T17:13:48Z"}

event: stage_complete
data: {"stage_name": "axe_core", "completed_at": "2026-03-17T17:14:10Z", "findings_count": 42}

event: stage_complete
data: {"stage_name": "image_audit", "completed_at": "2026-03-17T17:16:30Z", "findings_count": 8}

: keepalive

event: job_complete
data: {"job_id": "b5ea3ea7-...", "summary": {"violations": 20, "needs_review": 7, "passes": 45}}
```
</ResponseExample>

---

## Stage names reference

| `stage_name` | WCAG SC | Description |
|---|---|---|
| `axe_core` | multiple | Node.js / axe-core WCAG scan |
| `image_audit` | 1.1.1 | Image alt-text check (OCR-backed) |
| `form_audit` | 3.3.1, 3.3.2 | Form label and error message check |
| `label_in_name` | 2.5.3 | Accessible name must contain visible label |
| `pause_stop_hide` | 2.2.2 | Auto-playing / animated content check |
| `target_size` | 2.5.8 | Touch target minimum size check |